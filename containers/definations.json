[
  {
    "memory": 512,
    "cpu": 256,
    "secrets": [
      {
          "name": "AWS_SECRETS",
          "valueFrom": "${secret_arn}"
      }
    ],
    "essential": true,
    "image": "${repo_url}",
    "name": "${container_name}",
    "portMappings": [
      {
        "containerPort": "${container_port}",
        "hostPort": "${container_port}"
      }
    ]
  }
]